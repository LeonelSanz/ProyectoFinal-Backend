<section class="container">

    {{#if products}}
        <div class="container d-flex justify-content-around flex-wrap">
            {{#each products}}
                <div class="card mx-2 mb-4 shadow-sm" style="width: 15rem;">
                    <img
                        src={{this.image}}
                        alt="Product"
                        style="overflow: hidden;"
                    />
                    <div class="card-header">
                        <h5 class="card-title m-0">{{this.title}}</h5>
                    </div>
                    <div
                        class="card-body d-flex justify-content-between align-items-center"
                    >
                        <h4 class="card-title m-0">$ {{this.price}}</h4>
                        <a
                            href="/api/carts/product/{{this._id}}"
                        >
                            <button
                                class="btn m-0 btn-outline-dark rounded-circle"
                            >
                                <span class="">+</span>
                            </button>
                        </a>
                    </div>
                </div>
            {{/each}}
        </div>
    {{else}}
        <p class="alert alert-info" role="alert">There´s no products...</p>
    {{/if}}
</section>